import 'package:flutter/material.dart';

import '../sliding_top_panel.dart';

const Map<SlidingPanelDirection, AlignmentGeometry> directionToMap = {
  SlidingPanelDirection.top: Alignment.topCenter,
  SlidingPanelDirection.bottom: Alignment.bottomCenter,
};
